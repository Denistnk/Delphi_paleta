program Atividades;

uses
  Vcl.Forms,
  uCoresAleatorias in 'uCoresAleatorias.pas' {PaletaCores},
  uFundo in 'uFundo.pas' {frFundo};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TPaletaCores, PaletaCores);
  Application.CreateForm(TfrFundo, frFundo);
  Application.Run;
end.
